# Generated Code (sqlc)

This directory contains type-safe Go code generated by `sqlc`.

Do NOT edit these files manually — they are overwritten by `make sqlc`.

To (re)generate:
1. Install sqlc: https://docs.sqlc.dev/en/latest/overview/install.html
2. Edit SQL queries in `sqlc/queries/`
3. Run: `make sqlc`
